<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:colorPrimaryDark">@color/orange_app_dark</item>
        <item name="android:navigationBarColor">@color/orange_app_dark</item>
        <item name="android:windowNoTitle">true</item>

    </style>

    <style name="ThemeDialogTransparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/transparent</item>
    </style>

    <style name="AppThemeFullScreen" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

</resources>
